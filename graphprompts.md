1. Deal Flow Waterfall (D3.js)
Build an animated waterfall chart in D3.js showing how a private credit deal funnel narrows from sourced opportunities down to closed deals. Use a stark, high-contrast palette — deep charcoal background with chalk-white bars and a single accent color (electric amber or arterial red) to highlight the drop-off stages. Each bar should animate in sequentially on load with an eased transition, and hovering over a stage should expand a brutalist tooltip — no rounded corners, bold monospace font, hard drop shadow. Label every bar directly on the chart with percentage drop-off. No legend. Use a wide, landscape canvas (1400x600px) and give the chart room to breathe with generous padding. Title in a heavy serif, subtitle in a light sans-serif below it.

2. Yield Spread vs. Duration Scatter (Plotly)
Create an interactive scatter plot in Plotly where each point is a private credit instrument, plotted by duration on the x-axis and yield spread on the y-axis. Size each bubble by deal size and color it by credit tier (senior secured, unitranche, mezzanine, subordinated) using a deliberate, museum-quality palette — think Rothko: deep burgundy, warm ochre, slate blue, off-white. On hover, show a clean card-style tooltip with deal name, borrower industry, spread, and vintage year. Add a faint diagonal reference line representing the investment-grade public market equivalent. Use a white canvas with a single thin gray grid, no border box, and axis labels in a condensed grotesque typeface. Make the chart responsive and zoomable.

3. Interest Rate Sensitivity Heatmap (Seaborn + Matplotlib)
Design a heatmap in Seaborn showing portfolio sensitivity across a matrix of base rate scenarios (x-axis: SOFR from 3% to 8%) and borrower leverage multiples (y-axis: 3x to 7x EBITDA). Color encode interest coverage ratio using a diverging palette that shifts from a warm cream through neutral gray into a deep crisis red — the visual language of heat and distress. Annotate every cell with the ICR value in a small, tight monospace font. Remove all chart borders (spines), use only hairline gridlines, and set the title in a large, uppercase, wide-tracked sans-serif at the top left — flush, not centered. Export at 300dpi for print.

4. Vintage Year Performance Ribbon (D3.js animated GIF)
Build a flowing ribbon / alluvial diagram in D3.js that traces private credit fund cohorts (2015–2024 vintages) across lifecycle stages: deployment, harvesting, and realization. Each vintage is a colored ribbon whose width encodes capital deployed. Animate the ribbons growing left to right over a 4-second loop, with subtle undulation to suggest cash flow movement. Use a dark navy canvas, ribbons in gradient fills (cool teal to warm gold) that shift as they approach the realization stage. Capture as a looping GIF at 24fps. Minimize text — only vintage year labels on the left and stage labels at the top in a spare, editorial sans-serif.

5. Loan-to-Value Radial Chart (D3.js)
Create a radial bar chart in D3.js where each spoke represents a deal in the portfolio, its length encoding LTV ratio, and its color encoding the time since origination (light = recent, dark = seasoned). Arrange the spokes in order of industry sector, with subtle sector separators and sector labels placed along the outer ring in a clean, rotated sans-serif. The center of the chart should display a live-computed portfolio average LTV in a large numeral — stark, black, oversized, like a museum wall label. Add a slow rotation animation (full rotation over 60 seconds) to give it life as a dashboard centerpiece. White background, no fill inside the circle, thin stroke spokes.

6. Default Rate Timeline with Recession Bands (Plotly + D3 hybrid)
Build a line chart in Plotly showing private credit default rates from 2000 to present, overlaid with shaded recession bands (use NBER dates). The line itself should be thick (3px), rendered in a single confident ink-black. Recession bands in a warm, translucent terracotta. Annotate three to four key stress events (GFC, COVID, 2023 regional banking) with vertical callout lines and short editorial annotations — one sentence, set in italic, positioned like a newspaper marginalia note. Axis labels minimal — only years on x, only 0%, 2%, 4%, 6% on y. No chart border. Title large and left-aligned, as if it were a section header in a financial magazine.

7. Capital Structure Stack (Animated D3.js)
Build a stacked horizontal bar chart where each row is a deal, and segments represent the capital structure layers: revolver, senior secured term loan, unitranche, mezzanine, and equity. Assign each layer a color from a disciplined, five-color palette inspired by architectural material samples — concrete gray, raw linen, oxidized copper, matte black, chalk. On hover, each segment should lift slightly (CSS transform translateY) and display a brutalist tooltip with tranche size, rate, and maturity. Animate the bars building from left to right on load using a staggered delay per row. Sort rows by total deal size descending. Left-align the deal name labels in a tight, condensed typeface. Canvas should be tall and scrollable if needed — no compression.